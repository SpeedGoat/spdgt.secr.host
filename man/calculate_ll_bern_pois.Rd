% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_SMR.R
\name{calculate_ll_bern_pois}
\alias{calculate_ll_bern_pois}
\title{Calculate Log Likelihood for Bernoulli or Poisson Observations}
\usage{
calculate_ll_bern_pois(obstype, y, K, lambda, z)
}
\arguments{
\item{obstype}{Character. Observation type, either "bernoulli" or "poisson".}

\item{y}{Numeric. Number of observed captures/detections.}

\item{K}{Numeric. Number of sampling occasions or effort.}

\item{lambda}{Numeric. Baseline detection rate.}

\item{z}{Numeric. Availability indicator (typically 0 or 1).}
}
\value{
Numeric. The log likelihood value under the specified observation model.
}
\description{
Calculates the log likelihood for capture-recapture data under either a Bernoulli
or Poisson observation model, accounting for detection probability and availability.
}
\details{
The function computes log likelihoods using:

For Bernoulli model:
\itemize{
\item Detection probability p = 1 - exp(-lambda)
\item Log likelihood = log(Binomial(y | K, pz))
}

For Poisson model:
\itemize{
\item Log likelihood = log(Poisson(y | Klambda*z))
}

The z parameter typically indicates whether an individual is available
for detection (z=1) or not (z=0).
}
\examples{
# Bernoulli example
ll_bern <- calculate_ll_bern_pois(
  obstype = "bernoulli",
  y = 3,        # 3 detections
  K = 10,       # 10 occasions
  lambda = 0.5, # baseline rate
  z = 1         # individual available
)

# Poisson example
ll_pois <- calculate_ll_bern_pois(
  obstype = "poisson",
  y = 5,        # 5 detections
  K = 10,       # 10 occasions
  lambda = 0.5, # baseline rate
  z = 1         # individual available
)

}
